import { Action } from 'ketcher-core';
import type Editor from '../../Editor';
export declare function dropAndMerge(editor: Editor, mergeItems: any, action?: Action | null, copyAction?: Action): Action;
